import { IRenderer } from 'fela';
import { StaticStyles } from '../types';
export declare const renderStaticStyles: (renderer: IRenderer, styles: StaticStyles) => void;
export declare function renderAllStaticStyles(renderer: IRenderer): void;
